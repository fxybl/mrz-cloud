<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zqj.seata.order.mapper.OrderMapper">
    <insert id="create">
        insert into `order`(id, product_id, user_id, `count`, money, `status`)
        values (#{id}, #{productId}, #{userId}, #{count}, #{money}, #{status})
    </insert>
</mapper>